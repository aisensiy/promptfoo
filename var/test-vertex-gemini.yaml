description: "Verify Vertex Gemini still works after changes"

providers:
  - id: vertex:gemini-2.0-flash
    label: "vertex-gemini"

prompts:
  - label: "simple"
    raw: "What is 2+2? Reply with just the number."

tests:
  - vars: {}
    assert:
      - type: contains
        value: "4"
